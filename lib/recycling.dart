import 'package:flutter/material.dart';

class recycle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(child: Text('recycling'))
    );
  }
}